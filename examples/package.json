{
  "name": "h4b2-examples",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "h4b2": "file:../"
  },
  "scripts": {
    "build": "yarn install --force && tsc",
    "start": "yarn install --force && tsc && node build/app",
    "watch": "tsc -w"
  }
}
